export ZSH=/home/vagrant/oh-my-zsh

ZSH_THEME="robbyrussell"

plugins=(git git-extras sudo tmux bundler chucknorris)

source $ZSH/oh-my-zsh.sh

alias g="git"
alias sagu="sudo apt-get update"
alias sagi="sudo apt-get install"
